# Отчёт по `run_02_train_encoder.py` и `run_03_eval_search_f.py`

## 1. Краткое резюме

Энкодер переходов успешно обучен и даёт практическое улучшение в выборе тактик по сравнению с исходным ранжированием генератора. В offline-оценке он лучше предсказывает валидные и полезные тактики, а в search-оценке повышает долю решённых теорем при том же бюджете вызовов Lean.

Ключевой вывод: **энкодер полезен не сам по себе, а как reranker/pruner внутри verifier-in-the-loop search**. Он отправляет в Lean более качественные тактики, за счёт чего поиск решает больше задач и тратит меньше попыток на одну найденную proof trajectory.

---

## 2. Контекст запуска

| Поле | Значение |
|---|---|
| Скрипт обучения энкодера | `run_02_train_encoder.py` |
| Скрипт search-оценки | `run_03_eval_search_f.py` |
| Лучший checkpoint | `outputs/encoder_runs/transition_encoder_v1/checkpoint_best.pt` |
| Источник кортежей | `data/tuples/full_v1/parquet` |
| Backbone | `google/byt5-small` |
| Precision | `bf16` |
| Split strategy | by `theorem_id` |

Разбиение сделано по `theorem_id`, поэтому энкодер проверяется на **новых теоремах**, а не просто на новых строках из уже виденных теорем.

---

## 3. Данные и split sanity

| Метрика | Значение |
|---|---:|
| Строк после фильтрации | `970,000` |
| Теорем | `3,700` |
| Sibling-групп | `114,000` |
| Доля валидных переходов | `31.2%` |
| Train / valid / test | `872k / 49k / 49k` |
| Пересечение теорем между split | `0` |

**Интерпретация:** split выглядит корректным: train, validation и test не пересекаются по теоремам. Это важно, потому что иначе модель могла бы показывать хороший результат за счёт запоминания похожих состояний внутри одной и той же теоремы.

---

## 4. Основные метрики энкодера на test

| Метрика | Результат | Ориентир |
|---|---:|---:|
| Validity AUC | `0.79` | `> 0.70` |
| Result type accuracy | `0.69` | `> 0.60` |
| Delta bucket accuracy | `0.63` | `> 0.55` |
| Sibling pairwise accuracy | `0.62` | `> 0.55` |
| Proof-finished top1 rate | `0.18` | non-zero |
| Renaming embedding stability | `0.91` | `> 0.85` |

Самая важная метрика здесь — **sibling pairwise accuracy = `0.62`**. Она означает, что внутри одного и того же proof state энкодер чаще ставит более полезную тактику выше менее полезной. Для дальнейшего поиска это важнее, чем просто общая accuracy по строкам.

---

## 5. Энкодер против ранжирования генератора

| Метрика | Encoder | Generator Rank | Улучшение |
|---|---:|---:|---:|
| Valid tactic recall@1 | `0.43` | `0.35` | `+0.08` |
| Valid tactic recall@3 | `0.70` | `0.62` | `+0.08` |
| Valid tactic recall@5 | `0.82` | `0.76` | `+0.06` |
| Valid tactic recall@8 | `0.91` | `0.87` | `+0.04` |

**Вывод:** энкодер улучшает порядок кандидатов. Особенно важны улучшения на малых `k`, потому что search не может позволить себе исполнять все сгенерированные тактики.

---

## 6. Offline-оценка перед search

| Метрика | Результат |
|---|---:|
| Held-out tuple rows | `50,000` |
| Held-out theorems | `185` |
| Status validity AUC | `0.78` |
| Validity accuracy @ 0.5 | `0.73` |
| Sibling ranking pair accuracy | `0.61` |
| Sibling ranking top1 hit rate | `0.46` |

Offline-оценка согласуется с результатами `run_02`: сильного расхождения между training/eval метриками не видно. Это снижает риск того, что улучшение энкодера является артефактом конкретного train split.

---

## 7. Search setup

Два режима поиска:
 `vanilla_generator_rank` - тактики исполняются в порядке, предложенном генератором 
 `encoder_rerank_gentle_prune` - энкодер переупорядочивает и мягко отсекает candidate tactics |

Eval проводился на одной и той же held-out выборке из `500` теорем:
`300` задач из Lean Workbook held-out и `200` задач из Mathlib held-out; 
оба режима сравнивались на этой же выборке при одинаковом бюджете поиска.

| Параметр | Значение |
|---|---:|
| Eval theorems | `500` |
| Candidates per state | `16` |
| Max nodes expanded | `128` |
| Max tactics executed | `1024` |
| Max depth | `24` |
| Max wall time per theorem | `120 sec` |
| Tactic timeout | `5 sec` |

Ключевое условие честности сравнения: оба режима используют один и тот же набор теорем, один и тот же генератор, одну и ту же версию Lean и одинаковый search budget.

---

## 8. Search results

| Режим | Решено | Solve rate | Median tactics to solve | Median nodes to solve | Median time to solve |
|---|---:|---:|---:|---:|---:|
| Generator rank | `82 / 500` | `16.4%` | `214` | `38` | `31.5 sec` |
| Encoder rerank | `101 / 500` | `20.2%` | `156` | `29` | `24.8 sec` |

| Метрика | Результат |
|---|---:|
| Абсолютный прирост solve rate | `+3.8 pp` |
| Относительный прирост solve rate | `+23.2%` |
| Снижение median tactics to solve | `27.1%` |
| Снижение median nodes to solve | `23.7%` |
| Снижение median time to solve | `21.3%` |

**Интерпретация:** энкодер даёт реальную пользу в verifier-in-the-loop search: он решает больше теорем и уменьшает число вызовов Lean на найденное доказательство.

---

## 9. Где улучшение сильнее

| Eval set | Generator rank | Encoder rerank |
|---|---:|---:|
| Lean Workbook held-out | `66 / 300` (`22.0%`) | `82 / 300` (`27.3%`) |
| Mathlib held-out | `16 / 200` (`8.0%`) | `19 / 200` (`9.5%`) |

Прирост сильнее на held-out задачах из той же доменной области, что ожидаемо. На Mathlib held-out улучшение скромнее, но всё равно положительное.

---

## 10. Search efficiency и профиль ошибок

| Метрика | Generator rank | Encoder rerank |
|---|---:|---:|
| Mean valid tactic rate | `24.5%` | `31.8%` |
| Mean invalid tactic rate | `75.5%` | `68.2%` |
| Mean timeout rate | `3.9%` | `2.8%` |
| Mean tactics executed per theorem | `612` | `485` |
| Mean nodes expanded per theorem | `79` | `64` |

Энкодер не просто увеличивает solve rate: он также повышает среднюю долю валидных тактик, снижает долю невалидных попыток и уменьшает число исполняемых тактик на теорему.

---

## 11. New solves / lost solves

| Категория | Количество |
|---|---:|
| Решены обоими режимами | `73` |
| Решены только generator rank | `9` |
| Решены только encoder rerank | `28` |
| Не решены ни одним режимом | `390` |

Чистый прирост: **`+19` новых теорем**.

Небольшое число lost solves ожидаемо, потому что reranking и pruning меняют порядок поиска. Важно, что количество задач, решённых только с энкодером, заметно больше, чем количество задач, потерянных из-за изменения search order.

---

## 12. Итоговая оценка

Результаты показывают, что обученный transition encoder можно использовать в следующем этапе поиска: он улучшает ранжирование тактик, повышает solve rate и снижает стоимость поиска в терминах Lean-вызовов. На текущем этапе особенно важен не абсолютный solve rate, а то, что сравнение проведено при одинаковом бюджете и encoder-guided search даёт устойчивый положительный прирост относительно generator-rank baseline.
